name: finance
version: "1.0.0"
description: "Style pack for financial services and fintech. Professional, precise, compliant, and trustworthy."
author: StyleMCP
license: MIT

files:
  voice: voice.yaml
  copyPatterns: copy_patterns.yaml
  ctaRules: cta_rules.yaml
  tokens: tokens.json
  tests: tests.yaml

config:
  strictMode: true
  minScore: 70
