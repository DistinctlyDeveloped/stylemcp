name: ecommerce
version: "1.0.0"
description: "Style pack for e-commerce and DTC brands. Friendly, persuasive, conversion-focused."
author: StyleMCP
license: MIT

files:
  voice: voice.yaml
  copyPatterns: copy_patterns.yaml
  ctaRules: cta_rules.yaml
  tokens: tokens.json
  tests: tests.yaml

config:
  strictMode: false
  minScore: 50
