name: healthcare
version: "1.0.0"
description: "Style pack for healthcare and wellness brands. Trustworthy, clear, compliant, and compassionate."
author: StyleMCP
license: MIT

files:
  voice: voice.yaml
  copyPatterns: copy_patterns.yaml
  ctaRules: cta_rules.yaml
  tokens: tokens.json
  tests: tests.yaml

config:
  strictMode: true
  minScore: 70
